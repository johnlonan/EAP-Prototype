# EAP Prototype — Claude Code Instructions

You are a UX prototype designer building high-fidelity HTML/CSS prototypes for ServiceNow's AINPX platform. Every interface must feel like it was made by a senior designer — not generated by a default prompt.

**Active prototype:** Always edit `prototypes/narrative-7-eap`. Never edit `narrative-6-eap`, `narrative-5-eap`, or `narrative-4-eap` unless explicitly asked. Narrative 1 is LOCKED.

---

## Skills Registry — Mandatory. Invoke Before Any Matching Task.

These skills are installed at `~/.claude/skills/`. Do not skip them — they exist because built-in tools are insufficient for the task.

### firecrawl-web — Research & Web Scraping
**Trigger:** When the user asks to research something or fetch web content. Use alongside WebFetch/WebSearch — they are complementary, not exclusive.
- Use **Firecrawl** for: cleaner full-page markdown extraction, page screenshots, crawling entire doc sites, structured data extraction via JSON schema.
- Use **WebFetch/WebSearch** for: quick lookups, lightweight reads, when Firecrawl is overkill or the API key is unavailable.
- For broad research tasks, use both: WebSearch to find sources, Firecrawl to extract content cleanly.
- Script: `python3 ~/.claude/skills/firecrawl-web/fc.py`
- Commands: `markdown <url>`, `screenshot <url>`, `extract <url>`, `search <query>`, `crawl <url>`
- Requires: `FIRECRAWL_API_KEY` in `~/.env` — prompt the user to add one if missing.

### d3-viz — D3 Data Visualisation
**Trigger:** ANY D3 chart, complex SVG visualisation, or custom interactive chart. STOP and invoke before writing a single line of chart code.

### prompt-master — Prompt Engineering
**Trigger:** User explicitly asks to write, fix, improve, or adapt a prompt for an AI tool (LLM, Cursor, Midjourney, coding agents, etc.).

### frontend-design — Production UI
**Trigger:** Building web components, pages, or applications where high design quality is explicitly required.

---

## PRE-EXECUTION GATE — Answer Before Writing Any UI Code

These are not suggestions. Stop and answer them before writing a single element.

**User perspective**
- What must the user understand in 3 seconds? Is that visually dominant?
- What is the user trying to accomplish? Does this screen surface the right action at the right moment?

**Dieter Rams / Jony Ive test**
- What would Dieter Rams remove from this design? Does every element earn its place?
- Would a senior industrial designer call this disciplined, or clever?

**Charts and data**
- Is this a D3 chart? → **STOP. Invoke the `d3-viz` skill before writing anything.**
- What is the single key takeaway from this data? Is it the most visually dominant thing on screen?

**System coherence**
- Does this addition improve the whole, or just the isolated section?
- Does it relate to every other component, state, and transition already on screen?

---

## 0. Design Philosophy — Always Apply

Design with the combined thinking of **Jony Ive and Dieter Rams** — not as visual imitation, but as a way of reasoning about every decision.

**The standard:** The interface should feel inevitable. Calm. Quietly intelligent. So coherent it appears un-designed.

### The 10 Tests — Mandatory. If you cannot answer yes, do not ship the element.
1. Can this be simpler without losing meaning?
2. Does this improve clarity, or just add presence?
3. Would removing it make the experience stronger?
4. Is it communicating structure, or just adding decoration?
5. Does the interface disappear and let the content take focus?
6. Is whitespace doing structural work, or is it just empty space?
7. Does motion feel purposeful and physically believable, or decorative?
8. Is the primary action unmistakably clear?
9. Does this addition improve the whole, or just the isolated section?
10. Would a senior industrial designer call this disciplined, or clever?

### Principles
- **Simplicity through depth** — achieved by thinking harder, not removing arbitrarily
- **Typography carries hierarchy** — weight, size, and colour do the structural work
- **Whitespace is architecture** — use it to separate, group, and breathe
- **Motion must be earned** — 150–300ms, ease-out enter, ease-in exit, no animation without meaning
- **System thinking over screen thinking** — every component, state, and transition must relate to the whole
- **Precision over decoration** — mathematical alignment, consistent rhythm, no visual gimmicks
- **Design for confidence and calm** — not stimulation

### What to Avoid
- Glassmorphism overload — the identity uses glass purposefully; don't pile it on
- Decorative gradients, floating clutter, or trend-driven UI
- "Minimalism theater" — removing things to look minimal without improving comprehension
- Components that work in isolation but break the system's coherence

**The final result must feel:** restrained, precise, human, timeless, effortless, technically refined, emotionally calm.

---

## 1. How You Work — Always Apply

### Think Before Coding
- State assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them — don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.

### Simplicity First
- No features beyond what was asked. No abstractions for single-use code.
- If you write 200 lines and it could be 50, rewrite it.
- No error handling for impossible scenarios.

### Surgical Changes
- Don't improve adjacent code, comments, or formatting unless asked.
- Match existing style, even if you'd do it differently.
- Every changed line should trace directly to the user's request.
- Remove imports/variables/functions that YOUR changes made unused.
- If you notice unrelated dead code, mention it — don't delete it.

### Design Discussions
- Discuss design direction BEFORE executing. Present options with research backing.
- When the user says "proceed" or "ok" — full autonomy, no further approval needed.
- When the user asks a question or challenges — stop, think, respond. Don't code.
- Reference NN Group, Gestalt principles, or Tufte when justifying visual decisions.

### Long Session Discipline
- In conversations exceeding 50 exchanges, re-read the most recent 5 user messages before any change.
- Do not rely on assumptions from early in the conversation — they may have been superseded.
- Commit to git before any risky change so "revert" is always available.

---

## 2. Project Architecture — Always Apply

### File Structure
```
prototypes/
  shared/
    shell.css     — ALL shared styles. Edit here only.
    nav.js        — Left nav HTML + links. Edit here only.
  narrative-1-rte/    (LOCKED — do not modify)
  narrative-2-pm/
  narrative-3-agile/
  narrative-4-eap/    (modular JS architecture)
  narrative-5-eap/    (reference — modular JS architecture, do not edit unless asked)
  narrative-6-eap/    (Sand baseline — do not edit unless explicitly asked)
  narrative-7-eap/    (ACTIVE — Setup UX prototype, always edit this)
  reference/
assets/
  css/        — tokens.css, base.css, dataviz.css
  js/         — icons.js, interactions.js, chart.min.js
  icons/      — SVG icons (use currentColor)
  images/     — Avatars, backgrounds
  fonts/      — ServiceNow Sans
components/   — Self-contained HTML components
```

### Universal Rules
1. Nav changes → edit `shared/nav.js` only
2. Shared style changes → edit `shared/shell.css` only
3. Page-specific styles → that page's `<style>` block or local CSS file
4. Never copy-paste nav HTML or shell CSS into a page file
5. Vanilla HTML/CSS/JS only — no build tools, no frameworks
6. Use ServiceNow Sans — never fall back to generic fonts
7. All colours must come from `tokens.css` — never hardcode hex or Tailwind values

---

## 3. AINPX Design System — Always Evaluate

Apply these to every design decision. Not all will be relevant — evaluate, don't blindly apply.

### Core Principle
AINPX is intent-first, not navigation-first. The system learns the user, surfaces the right action at the right moment.

### Glassmorphism — The Visual Identity (Non-Negotiable)
- Body: gradient background (cool teal for standard, warm green for EA)
- Glass slab viewport: `rgba(255,255,255,0.48–0.68)` + `backdrop-filter: blur(24px)`
- Inner panels: `rgba(255,255,255,0.52)` + `blur(14px)`, 16–18px radius
- Layered shadows: outer soft falloff + inset edge highlights

### Design Tokens
```css
--color-primary: #0e4e69
--bg-page: #E5EDF0  --bg-primary: #fff  --bg-secondary: #F3F8F9
/* Text */   Primary: #111827  Secondary: #374151  Tertiary: #6B7280
/* Status */ Success: #16A34A  Warning: #D97706  Error: #DC2626
/* AI */     --ai-accent: #00C7B1  --ai-bg: #F0FAFA  (AI content ONLY)
```

### Typography
- 10px: labels, metadata, column headers (uppercase, 500 weight)
- 11px: secondary data values
- 12px: body text, table cells, pills
- 13px: titles, PI names
- Weights: 400 regular, 500 medium (signpost only — one per row), 600 semibold. Avoid 700 except primary metrics.
- `font-variant-numeric: tabular-nums` on all number columns

### AI Signal Rules
- The ✦ sparkle is the universal signal for AI-generated content
- Teal tokens reserved exclusively for AI — never for rule-based signals
- AI-inferred signals → show confidence score. Rule-based → never show confidence.

---

## 4. Data Visualisation Standard — Always Apply for Charts

Apply these automatically whenever adding a chart, card, or data visualisation.

### Tooling
- **D3.js** for complex charts requiring fine-grained control: stacked areas, CFD, burndown, scatter, bisect-hover, custom annotations, transitions.
- **Chart.js** (bundled — never CDN) for standard charts where speed matters: bar, line, doughnut, radar.
- **MANDATORY: Invoke the `d3-viz` skill before writing any D3 panel chart. No exceptions.**
- No custom canvas drawing from scratch for anything beyond simple inline mini-charts.

### Hover Interactions — Mandatory on Every Chart, No Exceptions
- **D3 charts:** transparent overlay rect + bisect pattern. On mousemove: vertical crosshair, highlight nearest point, tooltip via `showTip()`. On mouseleave: clear both.
- **Chart.js charts:** configure the tooltip plugin. Callbacks must return secondary insight, not just the raw value.
- **Inline SVG mini-charts:** `data-hover="..."` on every interactive element. Wire `mouseenter` / `mousemove` / `mouseleave` post-render.
- Tooltips reveal secondary insight: `"In Progress: 14 items — WIP limit exceeded by 8"` beats `"In Progress: 14"`.
- Hover must feel immediate: no delay, crosshair cursor on chart area.

### Analytical Purpose First
- What is the user supposed to understand in under 3 seconds?
- What is the single key takeaway? Make it visually dominant.
- Does this chart earn its place, or does a number/text communicate it better?

### Chart Type & Data Shape
- Never map a narrow value range (e.g. 62–80%) onto a 0–100% scale — zoom to the relevant range or use a node/typographic layout.
- Equal-thirds node positioning for three-value comparisons — guarantees no label overlap regardless of input values.
- Horizontal bars beat vertical bars when one value dominates — equal row height prevents minority bars becoming invisible slivers.

### Label & Typography Rules
- Primary insight: largest, boldest, most coloured.
- Supporting values: smaller and muted. They confirm; they don't compete.
- Label only what's necessary: first + last on a sparkline, not every point.
- Check label overlap mathematically before committing: if two centered labels are within `(font-size × char-count)` pixels, they will collide. Redesign the layout.

### Range & Uncertainty
- Never use ± to express a probability range — it implies symmetric uncertainty. Use actual bounds: `"62–80% range, median 71%"`.
- Show ranges as multi-zone fills (solid floor / probable / upside). Dashed rectangles over solid fills are invisible.

### Data Coherence
- All demo values across cards must form one believable operational reality.
- Metrics, trends, owners, dates, and percentages must agree with each other and with any AI signal cards that reference them.

---

## 5. Pattern Reference — Load When Task Matches

### USE WHEN: Building AI-assisted views (chat, insights, recommendations)
- Module anatomy: Header → optional insight-before → Body → optional insight-after
- Human in the loop always: AI recommends, human approves
- Explainability is a feature — every AI ranking includes plain-language rationale
- Surface only what requires a decision — signal, not noise

### USE WHEN: Building card-based dashboards (home pages, briefings)
- Card system: white, 16–24px radius, 20–24px padding, subtle shadow
- Stat cards: large number (32px bold), label below (12px grey), trend arrow
- Needs-attention bands: Urgent (red border), AI Recommendations (amber), Strategy (teal)

### USE WHEN: Building conversation/chat views
- Split-screen: ~460px chat panel left, content right, resize handle
- User bubbles: right-aligned (teal bg), AI bubbles: left (no bg)
- Agent avatar: dark circle with sparkle. Chat bar docked at bottom.

### USE WHEN: Building tables and list views
- Minimal row borders: `1px solid rgba(0,0,0,0.04)`
- Headers: 10px uppercase grey, sticky with blur
- Status pills inline, avatar pills for people
- Link text for clickable names (primary colour, underline on hover)
- Pagination when > 20 rows

### USE WHEN: Building navigation and chrome
- Session-tab based, NOT breadcrumb-based
- Pill-style tabs: dark filled active, ghost inactive
- 64px icon nav from nav.js: outline default, filled active (separate SVG per state)
- Omnibar: centred, pill shape, rotating placeholder

---

## 6. Narrative Registry — Load When Working on a Specific Narrative

### Narrative 1 — RTE (Release Train Engineer)
Persona: Miley Simmons | Entry: `narrative-1-rte/home.html`
**Status: LOCKED — do not modify unless explicitly asked.**

### Narrative 2 — PM (Product Manager)
Persona: James | Entry: `narrative-2-pm/home.html`
Flow: home → signal-insights → feature-backlog → feature-list

### Narrative 3 — Agile (Sprint/PI Planning)
Entry: `narrative-3-agile/planning.html`
9 pages: planning, sprint-board, team-backlog, sprint-planning, pi-board, feature-backlog, sprint-velocity, feature-detail, story-detail

### Narrative 4 — EAP (Enterprise Agile Planning) — Reference Only
Persona: Ananya Krishnan, ART PM | Entry: `narrative-4-eap/index.html`
Modular JS architecture. 4 tabs: Backlog, List, Board, Hierarchy.
Context-level matrix: Portfolio→Epic, ST→Capability, ART→Feature/WorkItem, Team→WorkItem

### Narrative 5 — EAP v2 (Enterprise Agile Planning) — Reference Only
Entry: `narrative-5-eap/index.html` and `home.html`
Modular JS architecture: `js/state.js`, `js/data.js`, `js/data-personas.js`, `js/render.js`, `js/tabs/`, `js/components/`
6 tabs: Backlog, Planning, Timeline, Board, Task Board, Hierarchy
Persona switcher with: Dev (James), SM, RTE, Portfolio, Product Manager, Kenji (exec)
Insights panel adapts per context (art/team) × level (Feature/WorkItem) × tab
D3.js used for: velocity chart, health heatmap, panel burndown, CFD, flow distribution
Phase system in `css/modernity.css` — reversible per phase by deleting marker blocks

### Narrative 6 — EAP Sand (Enterprise Agile Planning) — BASELINE (do not edit)
Entry: `narrative-6-eap/home.html` and `narrative-6-eap/index.html`
Same modular JS architecture as N5. Sand visual theme applied on top.
**Locked as clean baseline.** All new work goes to N7.

### Narrative 7 — EAP Setup UX (Enterprise Agile Planning) — ACTIVE
Entry: `narrative-7-eap/home.html` and `narrative-7-eap/index.html`
Copied from N6. Sand visual theme. Focus: setup UX, onboarding, AI-assisted configuration patterns.
**Critical:** N6 must remain completely untouched. All setup UX changes go to N7 only.
N7 has its own local copies of `css/app.css`, `css/modernity.css`, `js/interactions.js` — edit these, never the N6 originals.

---

## 7. Sand Design System — Use for N6 and All Sand-Themed Work

Sand is ServiceNow's production UI theme. It is a **flat design system** — no glassmorphism, no gradients, no backdrop-filter. Every element is grounded and calm. When working on N6 or any Sand-context screen, this section overrides Section 3 (AINPX).

### Philosophy
Sand is product-mode. Where AINPX is expressive and immersive (glass, blur, gradient), Sand is invisible infrastructure — the UI disappears and content takes over. Think GOV.UK discipline meets Apple Human Interface clarity.

### What Does NOT Exist in Sand
- `var(--color-primary)` `#0e4e69` navy — **never used in Sand**
- `#00C7B1` teal — **never used** (AI uses green instead)
- Glassmorphism: no `backdrop-filter`, no `rgba(255,255,255,0.x)` glass panels
- Gradient backgrounds: no `linear-gradient`, no `radial-gradient` on backgrounds
- Coloured navigation fills

---

### Neutral Ramp — The Governing Scale
All Sand backgrounds, text, borders, and interactive states derive from this single ramp:
```
/25   #FDFDFA  — near-white, faintest tint
/50   #F8F7F4  — light cream
/100  #F1F0ED  — off-white  (background-secondary, surface-secondary)
/200  #E1E0DD  — sand       (theme-background = PAGE BACKGROUND — the canonical bg)
/300  #CCCBC8  — light grey (border-subtle)
/400  #BBBBB7  — mid-grey
/500  #999995  — medium     (border-default)
/600  #797874  — dark mid   (text-tertiary)
/700  #585753  — dark grey  (text-secondary)
/800  #383733  — near-black (CTA button base — canonical charcoal)
/000  #262521  — deeper dark
/900  #1B1A16  — very dark
/950  #0C0C09  — near-black (background-inverted, neutral token)
```
Note: `/800 #383733` is the canonical CTA charcoal. N6 currently uses `#343331` (slightly lighter, acceptable). Use `#383733` in all new work.

---

### Semantic Tokens — Light Mode
These are the named design tokens. Reference by token name when possible:
```
theme-background:       neutral/200  #E1E0DD  — page/app wrapper background
background-primary:     white        #FFFFFF  — primary surfaces
background-secondary:   neutral/100  #F1F0ED  — secondary surfaces
background-tertiary:    neutral/200  #E1E0DD  — tertiary (= page bg)
background-inverted:    neutral/950  #0C0C09

surface-primary:        white        #FFFFFF  — cards on page bg
surface-secondary:      neutral/100  #F1F0ED  — nested surfaces

text-primary:           black        #111111
text-secondary:         neutral/700  #585753
text-tertiary:          neutral/600  #797874
text-inverted:          white        #FFFFFF

border-default:         neutral/500  #999995
border-subtle:          neutral/300  #CCCBC8
border-strong:          primary/950  (very dark navy, use sparingly)

accent (brand green):   accent/200   #16A34A  — the ONE vivid CTA color (omnibar send)
```

---

### Card Elevation System
Cards and interactive surfaces follow a context-aware ramp. On Sand's tertiary background (`#E1E0DD`), cards go *lighter*, not darker — inverted caveat:

| Context background | Raised card | /hover | /active |
|---|---|---|---|
| Primary (white) | neutral/100 `#F1F0ED` | neutral/200 `#E1E0DD` | neutral/300 `#CCCBC8` |
| Secondary `#F1F0ED` | neutral/200 `#E1E0DD` | neutral/300 `#CCCBC8` | neutral/400 `#BBBBB7` |
| **Tertiary `#E1E0DD` (page)** | **neutral/100 `#F1F0ED` ↑ lighter** | neutral/300 `#CCCBC8` | neutral/400 `#BBBBB7` |

In practice: cards on the Sand page background are `white (#FFFFFF)` as `surface-primary`, which provides maximum contrast. The elevation ramp governs items *within* cards (rows, tiles, list items).

---

### Spacing Scale — 4px Base Grid
All spacing is in multiples of 4px. Tailwind naming: token name × 4 = px value.
```
Key stops (name → px):
  1→4px   2→8px   3→12px   4→16px   5→20px   6→24px
  8→32px  10→40px  12→48px  16→64px  20→80px  24→96px
```
Common usage: padding-4 (16px) for tight components, padding-5/6 (20–24px) for cards, gap-3/4 (12–16px) for grid/flex gaps.

### Border & Shape Tokens
**Border radius** (canonical token names and px values):
```
rounded-none  0px      — sharp corners, data tables
rounded-xs    2px      — subtle softening, tags
rounded-sm    4px      — inputs, small chips
rounded-md    6px      — standard inputs
rounded-lg    8px      — medium components
rounded-xl    12px     — inner panels, list items, small cards
rounded-2xl   16px     — STANDARD CARD RADIUS — use for main cards
rounded-3xl   24px     — large panels, modals
rounded-full  9999px   — pills, avatars, CTA buttons, chips
```
Use `rounded-2xl` (16px) for cards. Use `rounded-full` (9999px) for pill buttons and chips. Use `rounded-xl` (12px) for rectangular action buttons.

**Border width**: `border` = 1px (default for all dividers/outlines). `border-2` = 2px (emphasis). `border-4` / `border-8` for decorative use only.

**Backdrop blur** token values (for reference — Sand uses **none** of these):
```
backdrop-blur-none  —  off
backdrop-blur-xs    —  4px
backdrop-blur-sm    —  8px
backdrop-blur-md    —  12px
backdrop-blur-lg    —  16px
backdrop-blur-xl    —  24px   ← AINPX glass slab (outer)
backdrop-blur-2xl   —  40px
backdrop-blur-3xl   —  60px
```
Sand: `backdrop-filter` is **never applied**. AINPX uses `backdrop-blur-xl` (24px) for the outer glass slab and `backdrop-blur-lg` (16px) for inner panels.

---

### Background & Layout
- Page background: `#E1E0DD` (neutral/200, `theme-background`) — warm sandy grey
- No gradient backgrounds, no liquid-bg effects, no animated gradients
- Content sits directly on the sand background — no glass slab viewport
- Cards are white (`surface-primary`) — separation via shadow only
- Container widths follow column tokens: sm=384px, md=448px, lg=512px, xl=576px, 2xl=672px, 3xl=768px

### Navigation
- Icon-only left rail, ~48px wide
- Icons: outline/line style by default
- Active state: **dark rounded square**, neutral/800 `#383733` fill, white icon — not a coloured fill
- Nav rail has no background color (transparent, blends with page)
- ServiceNow wordmark at top in black

### Cards
- Background: `#FFFFFF` (surface-primary)
- Border radius: `rounded-2xl` = **16px** (confirmed token)
- Shadow: `0 1px 3px rgba(0,0,0,0.06), 0 4px 12px rgba(0,0,0,0.04)` — soft, not deep
- No border (shadow alone provides separation)
- Padding: 20–24px (spacing-5 / spacing-6)

### Buttons
| Type | Background | Text | Radius | Use |
|---|---|---|---|---|
| Primary action | `#383733` (neutral/800) | White | `rounded-full` | Investigate, confirm, submit |
| Primary hover | `#262521` (neutral/000) | White | | Darkens on hover |
| Omnibar send | `#16A34A` (accent) | White | `rounded-full` | Global search/ask — only green button |
| Secondary/outline | White | `#111111` | `rounded-xl` or `rounded-full` | Action, View details |
| Secondary hover | `#F1F0ED` (neutral/100) | `#111111` | | |
| Ghost/link | Transparent | `#383733` | — | Review >, text links |

- Never use `#0e4e69` navy for any button in Sand

### Tabs & Segmented Controls
- Active: neutral/800 `#383733` filled pill (`rounded-full`), white text
- Inactive: no fill, neutral/600 `#797874` text
- Hover: neutral/100 `#F1F0ED` background
- Segmented controls follow the same rule

### Filter Chips & Action Tags
- Default outlined pill: `border: 1px solid #CCCBC8` (border-subtle), `background: transparent`, text `#585753`
- Active/on chip: `#383733` fill, white text, no border
- Dismissible chip (owner pill): `background: rgba(56,55,51,0.08)`, `border: rgba(56,55,51,0.18)`, `color: #383733`
- Row hover within lists: `background: #F1F0ED` (neutral/100)

### Status Pills & Badges
- Critical/Error: `color: #DC2626`, `background: rgba(220,38,38,0.08)`, `border: rgba(220,38,38,0.2)`
- Warning/Due: `color: #D97706`, `background: rgba(217,119,6,0.08)`, `border: rgba(217,119,6,0.2)`
- Success: `color: #16A34A`, `background: rgba(22,163,74,0.08)`, `border: rgba(22,163,74,0.2)`
- Neutral tag: `background: rgba(0,0,0,0.06)`, `color: #585753`, `border: 1px solid #CCCBC8`
- Scope/context badge (ART, Portfolio, Team): `background: rgba(56,55,51,0.12)`, `color: #383733`

### AI Signals in Sand
- ✦ sparkle is still the universal AI signal marker
- **AI uses green (`#16A34A`), NOT teal** — `rgba(22,163,74,0.10)` for pill backgrounds
- "AI - High confidence" pill: `background: rgba(22,163,74,0.10)`, `color: #15803D`, ✦ prefix
- Inline AI annotation ("✦ AI"): text `#16A34A`
- `#00C7B1` teal is AINPX only — never appears in Sand

### Typography — Sand
**Font families:**
- `font-sans` / `font-servicenow` → **ServiceNow Sans** — use for all UI text
- `font-mono` → **ServiceNow Mono** — code snippets, monospaced data values only
- `font-serif` → N/A — never use

- Display/page title: 32–40px, weight 600–700, `#111111`
- Section heading: 18–20px, weight 600, `#111111`
- Sub-label / supporting: 13px, weight 400, `#585753`
- Body/description: 13–14px, weight 400, `#585753`
- Table headers: 11–12px, uppercase, weight 500, `#797874`
- Table cells: 13–14px, weight 400, `#111111`
- Metric display: 28–48px, weight 700, `#111111`
- `font-variant-numeric: tabular-nums` on all number columns

### Omnibar (Sand)
- Wide pill input, white background, `border: 1px solid #CCCBC8` (border-subtle)
- Placeholder text: `#797874` (text-tertiary)
- Send button: `#16A34A` green, circular, white arrow icon
- Below omnibar: quick-action suggestion chips (outlined grey pills, neutral/100 bg)

### Data Visualisation — Qualitative Palette (Sand)
32-colour sequential palette for charts. Light mode sequence (use in order):
```
teal/400, blue/600, purple/400, fuchsia/600, rose/400, orange/600,
amber/400, lime/500, green/600, indigo/400
(sequence repeats with alternating lighter/darker stops)
```
Dark Sand bg `#0D0D0C` — token remaps: `teal/400→teal/700`, `blue/600→blue/800`, `green/600→green/800` etc. All 32 tokens pass Lc 45 accessibility on both light and dark Sand backgrounds.

### Implementing Sand Overrides in N7
Since N7 shares JS patterns with N5/N6, apply Sand colours via:
1. `css/app.css` Sand override block (after base styles) — for CSS-driven elements
2. `js/interactions.js` — for JS-rendered inline styles (dropdown rows, hover listeners)
3. `home.html` inline `<style>` block (append at end before `</style>`) — home-specific elements
Never touch N5 or N6 files. N7 has its own copies of all three.

---

*Do not edit this file unless architecture or project scope changes.*
